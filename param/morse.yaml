sim:
    ground_truth_localization: false
    ground_truth_altimeter: false

    # Choose one of MatteFloor, SharperMatteFloor, GymFloor, or RealFloor
    floor: MatteFloor

    # Uncomment to turn cameras on
    # front_camera_resolution: [1920, 1080]
    # left_camera_resolution: [1920, 1080]
    # right_camera_resolution: [1920, 1080]
    # bottom_camera_resolution: [1920, 1080]
